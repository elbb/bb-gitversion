# ===================================================
# images
# ===================================================

image=image-gitversion:
  image: "{env.BB_DOCKER_NAMESPACE}/bb-gitversion"
  context: gitversion
  tags: ["{env.GitVersion_BranchVersionDockerLabel}"]
