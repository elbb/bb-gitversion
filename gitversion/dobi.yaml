# ===================================================
# images
# ===================================================

image=image-gitversion:
  image: elbb/bb-gitversion
  context: gitversion
  tags: ["{env.GitVersion_MajorMinorPatch}"]

# ===================================================
# alias
# ===================================================

alias=gitversion-build:
  tasks:
    - image-gitversion