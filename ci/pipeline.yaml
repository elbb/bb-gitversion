resources:
  - name: source
    type: git
    source:
      uri: ((git_source))
      private_key: ((privatekey))
      branch: ((release_branch))
      # tag_filter: "*"
      # skip_ssl_verification: true

  - name: bb-gitversion
    type: docker-image
    source:
      repository: elbb/bb-gitversion
      tag: ci

  - name: ubuntu
    type: docker-image
    source:
      repository: ubuntu
      tag: 18.04

jobs:
  - name: Generate version and test it
    public: true
    plan:
      - in_parallel:
          - get: bb-gitversion
          - get: source
          - get: ubuntu
      - task: Generate version
        image: bb-gitversion
        config:
          platform: linux
          inputs:
            - name: source
          outputs:
            - name: gen
          run:
            path: /bin/bash
            args:
              - -exc
              - |
                entrypoint.sh
          params:
            GIT_PATH: df | grep source | awk  '{print $6}'
            GEN_PATH: find /tmp/build/ -type d -name "gen"

      - task: Test generated git version
        image: ubuntu
        config:
          platform: linux
          inputs:
            - name: gen
          run:
            path: /bin/bash
            args:
              - -exc
              - |
                cat gen/json/gitversion.json
