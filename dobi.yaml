# ===================================================
# alias
# ===================================================

alias=build:
  tasks:
  - image-gitversion
  annotations:
    description: "-> build gitversion container image"

alias=deploy:
  tasks:
  - image-gitversion:push
  annotations:
    description: "-> deploy gitversion container image"
